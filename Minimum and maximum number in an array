int Max(vector<int> v)
{
    int m=0;
    for(int i=0;i<v.size();i++)
    {
        if(v[i]>m)
            m=v[i];
    }
    return m;
}

int Min(vector<int> v)
{
    int m=v[0];
    for(int i=0;i<v.size();i++)
    {
        if(v[i]<m)
            m=v[i];
    }
    return m;
}
