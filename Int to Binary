//num to binary one
string Binary(int n)
{
    if(n==0)
        return "0";
    if(n%2==0)
        return Binary(n/2)+'0';
    else
        return Binary(n/2)+'1';
}

//quick methode
void Binary(int n)
{
    cout<<n%2;
    if(n==0)
        return;
    Binary(n/2);
}
